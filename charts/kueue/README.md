# kueue

Helm wrapper for [Kueue](https://kueue.sigs.k8s.io) with local templates bootstrapping default `ResourceFlavor`, `ClusterQueue`, `LocalQueue`, and `WorkloadPriorityClass` resources for batch job scheduling.


