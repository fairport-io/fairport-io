# nvidia-gpu-operator

Helm wrapper for the [NVIDIA GPU Operator](https://docs.nvidia.com/datacenter/cloud-native/gpu-operator) to provision and manage NVIDIA GPUs in Kubernetes (drivers, device plugin, monitoring, time-slicing, MIG).
