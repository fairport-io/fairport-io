# slurm

Helm wrapper for the [Slinky slurm chart](https://github.com/slinky-org/slinky) to deploy Slurm HPC clusters with configurable nodesets, GPU support via Gres, and OCI container runtime integration (pyxis/enroot).

